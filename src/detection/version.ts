/**
 * Semver version string for the LocalDetectionPipeline.
 * Increment when pipeline logic changes in a way that affects detection outputs
 * so that audit logs can identify which pipeline version produced a result.
 */
export const PIPELINE_VERSION = '1.0.0';
