{
  "name": "obfusca-extension",
  "version": "0.1.0",
  "description": "LLM Data Loss Prevention - Prevent sensitive data leaks to ChatGPT",
  "type": "module",
  "scripts": {
    "dev": "node build.mjs --watch",
    "build": "tsc && node build.mjs",
    "build:vite": "vite build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.260",
    "@webgpu/types": "^0.1.69",
    "typescript": "^5.3.3",
    "vite": "^5.0.12",
    "vitest": "^1.2.2"
  },
  "dependencies": {
    "@mlc-ai/web-llm": "^0.2.82",
    "onnxruntime-web": "^1.24.3"
  }
}
